layui.use(['laypage', 'layer', 'table', 'element', 'form'], function () {
    let layer = layui.layer,
        $ = layui.jquery;

    //向世界问个好
    // layer.msg('进入文件夹');

    // form.render(null, 'filelistbox');

    // 一级目录
    parent.window.folderHome()
});
